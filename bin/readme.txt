How to set up the binaries
-----------------------------

The paths are hardcoded so you have to store the binaries like this:
->    Windows = "ffmpeg-windows/bin/ffmpeg.exe"
->    Linux = "ffmpeg-linux/ffmpeg"